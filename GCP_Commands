1. gcloud config get-value project
2. gcloud projects list
3. gcloud config set project [YOUR_PROJECT_ID]
4. gcloud services enable aiplatform.googleapis.com storage.googleapis.com compute.googleapis.com
5. gcloud auth application-default print-access-token > /dev/null 2>&1 && echo "ADC Configured" || echo "ADC Not Configured"
6. gcloud auth application-default login
7. gcloud storage buckets create gs://minfyhr-rag-test-data --location=us-central1 || echo "Bucket might already exist or name taken"
8. gcloud storage buckets list

